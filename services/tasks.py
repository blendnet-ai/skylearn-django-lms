# from celery import shared_task
# from .resume_builder_service import ResumeBuilderService


# @shared_task(queue='services_queue')
# def build_resume_task(user_id ,resume_id):
#     ResumeBuilderService().build_resume(user_id,resume_id)
    
    